[project]
name = "miniann-transformer"
version = "0.1.0"
description = "A minimal NumPy-only decoder Transformer for education and research."
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[tool.setuptools.packages.find]
include = ["miniann_transformer*"]
